{
  "name": "helical-process",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npx serve .",
    "start": "npx serve . -l $PORT",
    "build": "echo 'Static site — no build needed'"
  },
  "dependencies": {
    "serve": "^14.2.1"
  }
}
